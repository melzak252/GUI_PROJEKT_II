package projekt.game.components.ships.configs;

import projekt.game.components.ships.configs.EnemyConfig;

public class TankConfig extends EnemyConfig {


    public TankConfig(Integer row, Integer col) {
        super(row, col);
    }
}